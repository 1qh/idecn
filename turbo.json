{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "env": ["NEXT_DIST_DIR"],
      "outputs": [".next/**", "dist/**"]
    },
    "check": {
      "dependsOn": ["build"],
      "cache": false
    },
    "fix": {
      "dependsOn": ["build"],
      "cache": false
    },
    "test": {
      "cache": false,
      "env": ["E2E_PORT"]
    }
  }
}
