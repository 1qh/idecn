{
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "bun run --filter '*' build",
    "check": "lintmax check",
    "dev": "bun run --filter '@nicetree/demo' dev",
    "fix": "lintmax fix && bun run build"
  },
  "devDependencies": {
    "lintmax": "latest",
    "typescript": "latest"
  }
}
